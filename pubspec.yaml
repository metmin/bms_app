name: bmi_calculator
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  font_awesome_flutter: ^8.4.0
  flutter_speedometer: ^1.0.2
  scoped_model: ^2.0.0-nullsafety.0
  sensors_plus: ^1.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_bluetooth_serial:
    path: C:\Users\metmi\bms\flutter_bluetooth_serial2
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true